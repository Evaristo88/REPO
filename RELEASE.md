# Release v0.1.0

Release: v0.1.0
Date: 2026-02-05

Includes:
- Added MIT `LICENSE`.
- Added `.gitignore`.
- Implemented `starter_log_analyzer.py` with parsing, follow mode, blocklist and optional auto-blocking.
- Hardened `starter_password_checker.py` (masked input and strength checks).
- Extended `starter_linux_security_check.sh` with security checks.
- Added unit tests and `run_tests.sh` test runner.
- Added `LAB_DOCUMENTATION.md` and `README.md` with usage and safety notes.

Notes:
- This initial release is intended for educational use. See `LAB_DOCUMENTATION.md`
  for usage instructions and safety guidance before enabling auto-blocking.
